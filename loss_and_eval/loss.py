import torch
from config import device


def loss(a, b, alpha=0.1):
    r"""
    Loss in paper.
    Args:
        a: [batch_size, len, dim]
        b:
        alpha: blend factor
    Returns:
        A value.
    """
    
    pass